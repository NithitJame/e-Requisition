// Re-export shim. The filter form now lives in shared/components/PromotionListingFilters
// (shared by AllPA, AllTA, Approve). Kept here so existing pa imports keep working.
export { default } from '@/shared/components/PromotionListingFilters';
